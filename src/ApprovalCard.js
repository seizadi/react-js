import React from 'react';

const ApprovalCard = () => {
    return (
        <div>

            <button >Approve</button>
            <button >Reject</button>
        </div>
    );
}

export default ApprovalCard;
